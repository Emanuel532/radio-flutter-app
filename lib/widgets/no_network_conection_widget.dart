import 'package:flutter/material.dart';

class NoNetworkConnectionWidget extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Fara internet'),
    );
  }
}
